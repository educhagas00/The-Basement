<form action="{{ action }}" class="text-timberwolf border border-timberwolf rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-rich-black-dark sm:text-sm font-semibold"
@if(!method)
    method="POST"
@else
    method="{{ method }}"
@end
>
    {{{ await $slots.main() }}}
</form>